#include <stdio.h>
#include <iostream>
#include <string.h>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
#include <math.h>
#define ms(a,b) memset(a,b,sizeof(a))
#define inf 1<<28
#define sz
using namespace std;
int main ()
{
    int test,t;
    double a,b,c,ratio1,khetro1,khetro2;
    cin>>test;
    for(t=1;t<=test;t++)
    {
        cin>>a>>b>>c>>ratio1;
    }
    return 0;
}
